@using MudBlazor

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Delete" Class="mr-3" Color="Color.Error" /> Delete Member
        </MudText>
    </TitleContent>

    <DialogContent>
        @if (!string.IsNullOrEmpty(errorMessage))
        {
            <MudAlert Severity="Severity.Error" Class="mb-4">@errorMessage</MudAlert>
        }

        @if (isLoading)
        {
            <div class="d-flex justify-center pa-6">
                <MudProgressCircular Indeterminate="true" Size="Size.Large" />
            </div>
        }
        else if (member is not null)
        {
            <MudText Typo="Typo.body1" Class="mb-2"><strong>Name:</strong> @member.Name</MudText>
            <MudText Typo="Typo.body1" Class="mb-2"><strong>Code:</strong> @member.MemberCode</MudText>
            <MudText Typo="Typo.body1" Class="mb-4"><strong>Created:</strong> @member.CreatedAt.ToString("yyyy-MM-dd HH:mm")</MudText>

            <MudAlert Severity="Severity.Warning" Variant="Variant.Outlined">
                Are you sure you want to delete this member?
            </MudAlert>
        }
    </DialogContent>

    <DialogActions>
        <MudButton OnClick="Cancel" Variant="Variant.Text">Cancel</MudButton>
        <MudButton OnClick="ConfirmDelete"
                   Variant="Variant.Filled"
                   Color="Color.Error"
                   Disabled="@isDeleting">
            @if (isDeleting)
            {
                <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" />
            }
            Yes, Delete
        </MudButton>
    </DialogActions>
</MudDialog>
