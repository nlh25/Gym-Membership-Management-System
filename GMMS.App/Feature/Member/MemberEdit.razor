@using MudBlazor

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Edit" Class="mr-3" /> Edit Member
        </MudText>
    </TitleContent>

    <DialogContent>
        @if (!string.IsNullOrEmpty(errorMessage))
        {
            <MudAlert Severity="Severity.Error" Class="mb-4">@errorMessage</MudAlert>
        }

        @if (isLoading)
        {
            <div class="d-flex justify-center pa-6">
                <MudProgressCircular Indeterminate="true" Size="Size.Large" />
            </div>
        }
        else
        {
            <MudTextField @bind-Value="request.MemberCode"
                          @oninput="OnMemberCodeChanged"
                          Label="Member Code"
                          Variant="Variant.Outlined"
                          Required="true"
                          RequiredError="Member Code is required."
                          MaxLength="50"
                          Class="mb-3"
                          HelperText="Uppercase letters, numbers, and hyphens only (max 50 chars)" />

            <MudTextField @bind-Value="request.Name"
                          Label="Name"
                          Variant="Variant.Outlined"
                          Required="true"
                          RequiredError="Name is required."
                          MaxLength="100"
                          Class="mb-3"
                          HelperText="Max 100 characters" />
        }
    </DialogContent>

    <DialogActions>
        <MudButton OnClick="Cancel" Variant="Variant.Text">Cancel</MudButton>
        <MudButton OnClick="Update"
                   Variant="Variant.Filled"
                   Color="Color.Primary"
                   Disabled="@isSaving">
            @if (isSaving)
            {
                <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" />
            }
            Update
        </MudButton>
    </DialogActions>
</MudDialog>